language: python

matrix:
  include:
    - python: '3.5'
    - python: '3.6'
    - python: '3.7-dev'
    - python: '3.7'
      dist: xenial
      sudo: true

cache:
  pip: true
  directories:
    - $HOME/.pylint.d

install:
  - pip install -r requirements-devel.txt

script:
  - make travis
